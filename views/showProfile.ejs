<!DOCTYPE html>
<html lang="en">

<head>
    <style>
        :root {
            --primary-color: #5c48ee;
            --secondary-color-dark: #0f1e6a;
            --secondary-color: #f9fafe;
            --text-color: #333333;
            --white: #ffffff;
            --max-width: 1200px
        }

        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppin', sans-serif;
            background-color: var(--secondary-color);
        }

        nav {
            max-width: var(--max-width);
            margin: auto;
            padding: 1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .navbar-logo {
            font-size: 1.5rem;
            font-weight: 500;
            color: var(--primary-color);
            transition: .3s;
        }

        .navbar-logo:link {
            text-decoration: none;
        }

        .navbar-links {
            list-style: none;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .link a {
            padding: 0.5rem 1.5rem;
            font-size: 1rem;
            font-weight: 750;
            color: var(--text-color);
            text-decoration: none;
            transition: 0.3s;
        }

        .link a:hover {
            color: var(--primary-color);
        }

        .link .navbar-btn {
            padding: .5rem 2rem;
            color: var(--white);
            background-color: var(--primary-color);
            border-radius: 5px;
        }

        .link .navbar-btn:hover {
            background-color: var(--secondary-color-dark);
        }

        table {
            font-family: 'Poppin', sans-serif;
            font-size: 20px;
            margin-top: 5vh;
            margin-left: auto;
            margin-right: auto;
        }

        th {
            color: var(--primary-color);
            padding-top: 20px;
            padding-bottom: 20px;
            padding-left: 40px;
            padding-right: 40px;
            border-bottom: 1px solid;
            border-top: 1px solid;
        }

        td {
            color: var(--text-color);
            padding-top: 20px;
            padding-bottom: 20px;
            padding-left: 40px;
            padding-right: 40px;
            border-bottom: 1px solid;
        }

        .btn-details .detail-link {
            padding: .5rem 2rem;
            color: var(--white);
            background-color: red;
            border-radius: 5px;
            text-decoration: none;
        }

        .btn-details .detail-link:hover {
            background-color: orange;
            text-decoration: none;
        }

        .content-container {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .content-container form {
            display: flex;
            align-items: center;
        }

        .content-container input {
            width: 100%;
            max-width: 500px;
            padding: 1rem;
            border: none;
            outline: none;
            font-size: 0.8rem;
            box-shadow: 5px 5px 20px rgba(0, 0, 0, 0.2);
            margin-left: 50%;
            margin-top: 20px;
        }

        .content-container button {
            width: fit-content;
            padding: 1rem;
            font-size: .8rem;
            white-space: nowrap;
            background-color: var(--secondary-color-dark);
            color: var(--white);
            border: none;
            outline: none;
            box-shadow: 5px 5px 20px rgba(0, 0, 0, 0.2);
            transition: 0.3s;
            cursor: pointer;
            margin-top: 20px;
        }

        .content-container button:hover {
            background-color: var(--primary-color);
        }

        .content-container2 {
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .content-container2 button {
            width: fit-content;
            padding-top: 1rem;
            padding-bottom: 1rem;
            padding-left: 2rem;
            padding-right: 2rem;
            font-size: .8rem;
            white-space: nowrap;
            background-color: var(--primary-color);
            color: var(--white);
            border: none;
            outline: none;
            box-shadow: 5px 5px 20px rgba(0, 0, 0, 0.2);
            transition: 0.3s;
            cursor: pointer;
            margin-top: 20px;
            margin-left: 50%;
        }

        .content-container2 button:hover {
            background-color: var(--secondary-color-dark);
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hacktiv Learn</title>
</head>

<body>
    <nav>
        <div class="navbar-logo"><a href="/"> HacktivLearn </a></div>
        <ul class="navbar-links">
            <li class="link"><a href="/"> Home </a></li>
            <li class="link"><a href="/categories"> Category </a></li>
            <li class="link"><a href="/courses"> Courses </a></li>
            <li class="link"><a href="/profiles"> Profile </a></li>
            <li class="link"><a href="/logout" class="navbar-btn"> Log Out </a></li>
        </ul>
    </nav>
    <div class="content-container">
        <form role="search" action="/profiles" method="get">
            <input type="text" placeholder="Whom do you seek?" name="fullName">
            <button type="submit"> Search </button>
        </form>
    </div>

    <div class="content-container2">
        <button><a href="/profiles/add" style="color: white; text-decoration: none;">Add New User</a></button>
    </div>

    <% if(name) { %>
        <p style="color: red; text-align: center; margin-top: 30px;">
            <%= name %> removed
        </p>
        <% } %>

            <table>
                <thead class="head-table">
                    <tr>
                        <th scope="col">No</th>
                        <th scope="col">Name</th>
                        <th scope="col">Gender</th>
                        <th scope="col">Status</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>

                <tbody>
                    <% data.forEach((item, i)=> { %>
                        <tr>
                            <td>
                                <%= i + 1 %>
                            </td>
                            <td>
                                <%= item.fullName %>
                            </td>
                            <td>
                                <%= item.gender %>
                            </td>
                            <td>
                                <%= item.formatDate %>
                            </td>
                            <td>
                                <button class="btn-details"><a
                                        onclick="return confirm(`Are you sure you want to delete?`)" class="detail-link"
                                        href="/profiles/<%= item.id %>/delete">
                                        Delete </a></button>
                            </td>
                        </tr>
                        <% }); %>

                </tbody>
            </table>

</body>

</html>